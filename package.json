{
  "name": "termin-direkt-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "cd server && npx nodemon server.js",
    "client": "cd client && npx react-scripts start",
    "start": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.11",
    "react-scripts": "^5.0.1"
  }
}
